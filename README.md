# PW-Extractor (Physics Wallah Only)

Full Video (M3U8) + Notes Extractor Bot with Premium System

**Deploy on Render (Free):**
1. Connect repo
2. Build Command: `pip install -r requirements.txt`
3. Start Command: `bash start.sh`
4. Add all env vars from config.py
5. Done ✅
